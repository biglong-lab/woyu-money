<!-- CLAUDE_CODE_PROJECT_TEMPLATE_V5_HUNG -->
<!-- 當 Claude Code 偵測到此檔案，應自動開始專案初始化流程 -->

<!-- 模板資訊 -->
<!-- 原始模板作者: Chang Ho Chien | HC AI 說人話channel -->
<!-- 優化版本: Hung (大哉實業) -->
<!-- 整合來源: everything-claude-code (affaan-m) -->
<!-- 模板版本: 5.0.0 -->
<!-- 優化日期: 2026-02-02 -->

# 🚀 專案自動初始化模板（終極完整版）

**原始模板 by Chang Ho Chien | HC AI 說人話channel**
**優化版本 by Hung | 大哉實業有限公司 | v5.0.0**
**整合 everything-claude-code by affaan-m（Anthropic 黑客松冠軍）**
📺 原始教學: https://youtu.be/8Q1bRZaHH24
🔗 everything-claude-code: https://github.com/affaan-m/everything-claude-code

---

## 🌐 語言設定（最高優先級）

> **⚠️ 重要：Claude Code 必須遵守以下語言規則 ⚠️**

### 📢 強制中文對話
- **所有對話必須使用繁體中文**
- **所有提問必須使用繁體中文**
- **所有說明必須使用繁體中文**
- **所有錯誤訊息必須使用繁體中文解釋**
- **commit 訊息使用繁體中文**
- **程式碼註解使用繁體中文**
- **只有程式碼語法本身使用英文**

### 📢 對話風格
- 簡潔明瞭，不要太多廢話
- 先做事，再解釋
- 遇到問題直接說怎麼解決
- 不要一直問「您要我...嗎？」，直接做
- 技術選擇要說明原因

---

## 📊 進度追蹤（類 Replit 模式）

> **每次開啟專案或開始新對話時，Claude Code 必須：**
> 1. 先讀取 `PROGRESS.md` 了解目前進度
> 2. 告訴使用者目前狀態和下一步建議
> 3. 完成任務後更新 `PROGRESS.md`

---

## 🤖 初始化流程

### 步驟一：收集專案資訊（用中文問）

```
Claude Code 應該問：

1. 「專案名稱是什麼？」 → [PROJECT_NAME]

2. 「簡單描述這個專案的用途？」 → [PROJECT_DESCRIPTION]

3. 「專案類型？」
   - 簡單型（基本工具/腳本/API）
   - 標準型（完整應用程式）
   - SaaS 型（多租戶訂閱制服務）

4. 「網站類型？」
   - 開放型（需要 SEO、AI 爬蟲優化）
   - 封閉型（登入才能用、防爬蟲）
   - 混合型（部分開放、部分封閉）

5. 「需要哪些功能？」（可複選）
   - 會員系統（帳號管理）
   - Firebase 登入
   - LINE 登入
   - Cloudinary 圖片管理
   - Resend 信件發送
   - 資料庫
   - API 介面
   - 管理後台
   - 自動化檢測（CI/CD）

6. 「要設定 GitHub 備份嗎？」
   - 是，建立新的 GitHub 倉庫
   - 是，連接現有的 GitHub 倉庫
   - 否，之後再設定
```

---

# CLAUDE.md - [PROJECT_NAME]

> **文件版本**: 5.0
> **最後更新**: [DATE]
> **專案名稱**: [PROJECT_NAME]
> **專案描述**: [PROJECT_DESCRIPTION]
> **網站類型**: [開放型/封閉型/混合型]

---

## 🚨 最高優先規則

### 🌐 語言規則（最優先）
> **Claude Code 必須回應：**
> 「✅ 我會全程使用繁體中文對話」

### 🔄 規則確認
> **開始任何任務前，Claude Code 必須回應：**
> 「✅ 已確認 CLAUDE.md 規則，我會遵守所有規範」

---

# ═══════════════════════════════════════════
# 第一部分：開發流程與部署
# ═══════════════════════════════════════════

## 🔄 開發 → 備份 → 部署 完整流程

```
┌─────────┐    push     ┌─────────┐   webhook   ┌─────────┐
│ 你的 Mac │ ──────────▶ │ GitHub  │ ──────────▶ │ Coolify │
│ 本地開發 │  功能完成時  │ 雲端備份 │   自動觸發   │ 自動部署 │
└────┬────┘            └─────────┘             └────┬────┘
     │                                              │
     │  commit（頻繁）                                ▼
     │  本地存檔                                 ┌─────────┐
     ▼                                          │ 伺服器   │
┌─────────┐                                     │ 網站上線 │
│ 本地 Git │                                     └─────────┘
│ 完整歷史 │
└─────────┘
```

### 三個關鍵動作

| 動作 | 指令 | 目的地 | 時機 |
|------|------|--------|------|
| **commit** | `git commit` | 本地 Git | 隨時，頻繁 |
| **push** | `git push` | GitHub | 功能完成時 |
| **deploy** | 自動 | 伺服器 | push 後 Coolify 自動處理 |

---

## 💾 備份策略

### 本地端（草稿區）
- ✅ 隨時 commit（不限次數）
- ✅ 保留完整開發歷史
- ✅ 定期清理（保留最近 30 天）

### GitHub（正式版本）
- ✅ 只推功能完成的版本
- ✅ 上線版本用 tag 標記
- ✅ 每週至少 push 一次（安全底線）

### Commit 時機

| 時機 | 該 commit 嗎 |
|------|-------------|
| 完成一個小功能 | ✅ 是 |
| 修復一個 bug | ✅ 是 |
| 重構完成 | ✅ 是 |
| 休息/離開前 | ✅ 是 |
| 改一行程式 | ❌ 太瑣碎 |
| 功能做一半 | ⚠️ 加 WIP: |

### Push 時機

| 時機 | 該 push 嗎 |
|------|-----------|
| 功能開發完成 | ✅ 是 |
| 測試通過 | ✅ 是 |
| 每週五下班前 | ✅ 安全備份 |
| 開發中半成品 | ❌ 等完成 |

### 版本標記

```bash
git tag -a v1.0.0 -m "第一版正式上線"
git push origin v1.0.0
# 版本號：v主版本.功能版本.修復版本
```

---

## 📝 Commit 訊息規範

| 類型 | 說明 | 範例 |
|------|------|------|
| feat | 新功能 | `feat: 新增會員登入功能` |
| fix | 修復 | `fix: 修復密碼重設問題` |
| refactor | 重構 | `refactor: 重構使用者模組` |
| style | 樣式 | `style: 調整按鈕顏色` |
| docs | 文件 | `docs: 更新 API 文件` |
| test | 測試 | `test: 新增登入測試` |
| chore | 雜項 | `chore: 更新依賴套件` |
| WIP | 進行中 | `WIP: 上傳功能開發中` |

---

## 🌿 分支策略

```
main（正式版）← 只從 develop 合併
  │
develop（開發版）
  ├── feature/xxx    ← 新功能
  ├── bugfix/xxx     ← 修復 bug
  └── hotfix/xxx     ← 緊急修復
```

---

# ═══════════════════════════════════════════
# 第二部分：程式碼規範
# ═══════════════════════════════════════════

## 📏 程式碼結構規範

### 行數限制

| 等級 | 行數 | 說明 |
|------|------|------|
| 🟢 最佳 | 500 行以內 | 理想狀態 |
| 🟡 可接受 | 500-800 行 | 可以但要注意 |
| 🟠 警告 | 800-1000 行 | 應該拆分 |
| 🔴 禁止 | 超過 1000 行 | 必須拆分 |

### 函式/類別限制

| 項目 | 上限 |
|------|------|
| 單一函式 | 50 行 |
| 單一類別 | 300 行 |
| 單一檔案 | 800 行 |
| 巢狀層數 | 最多 3 層 |

---

## 🧩 模組化設計規範

### 核心原則

```
1. 一個功能 = 一個模組
2. 模組之間透過介面溝通（index.js 導出）
3. 禁止跨模組直接引用內部檔案
4. 共用功能放 shared/
5. 低耦合、高內聚
```

### 模組結構

```
modules/
├── auth/                   # 認證模組
│   ├── index.js            # 模組入口（唯一對外介面）
│   ├── auth.controller.js
│   ├── auth.service.js
│   ├── auth.routes.js
│   ├── auth.middleware.js
│   ├── auth.validator.js
│   └── auth.test.js
├── users/
├── upload/                 # Cloudinary
├── email/                  # Resend
└── ...
```

### 耦合度規則

| 等級 | 跨模組引用數 | 狀態 |
|------|-------------|------|
| 🟢 低耦合 | 0-1 | 正常 |
| 🟡 中耦合 | 2-3 | 注意 |
| 🔴 高耦合 | 4+ | 必須重構 |

### 依賴注入

```javascript
// ❌ 禁止硬耦合
this.userService = require('../users/user.service');

// ✅ 正確：透過入口
const { service: userService } = require('../users');

// ✅ 更好：依賴注入
class OrderService {
  constructor(userService) {
    this.userService = userService;
  }
}
```

---

## 📝 命名規範

### 檔案命名

| 類型 | 格式 | 範例 |
|------|------|------|
| 模組檔案 | `{模組}.{類型}.js` | `auth.controller.js` |
| 元件 | `PascalCase.jsx` | `UserCard.jsx` |
| 工具 | `camelCase.js` | `formatDate.js` |
| 常數 | `UPPER_CASE.js` | `ERROR_CODES.js` |

### 變數命名

```javascript
// 布林值：is/has/can/should 開頭
const isActive = true;

// 陣列：複數
const users = [];

// 函式：動詞開頭
function getUser() {}
function createOrder() {}

// 常數：全大寫
const MAX_RETRY_COUNT = 3;
```

### 禁止的命名
```javascript
// ❌ data, temp, a, b, c, v2, new, enhanced
// ✅ userData, formattedDate, index, count
```

---

## 📖 註解規範（JSDoc）

```javascript
/**
 * 根據 ID 取得使用者資料
 *
 * @param {number} userId - 使用者 ID
 * @param {Object} options - 查詢選項
 * @returns {Promise<User|null>} 使用者物件或 null
 * @throws {ValidationError} 當 userId 無效時
 */
async function getUserById(userId, options = {}) {
  // ...
}
```

---

## 🛡️ 輸入驗證規範

```javascript
// 使用 Joi 統一驗證層
const validate = (schemaName) => {
  return (req, res, next) => {
    const { error } = schemas[schemaName].validate(req.body);
    if (error) return res.status(400).json({ ... });
    next();
  };
};

router.post('/users', validate('createUser'), controller.create);
```

---

## 📊 日誌規範

```javascript
// ✅ 正確
logger.info('使用者登入成功', { userId: user.id });
logger.error('資料庫查詢失敗', { error: err.message });

// ❌ 禁止
console.log('user logged in');
```

| 等級 | 用途 |
|------|------|
| error | 錯誤、異常 |
| warn | 警告 |
| info | 重要事件 |
| debug | 除錯資訊 |

---

## 🔧 重構規則

### 何時重構

| 信號 | 行動 |
|------|------|
| 檔案超過 500 行 | 拆分模組 |
| 函式超過 50 行 | 拆分函式 |
| 複製貼上程式碼 | 抽取共用 |
| 超過 3 層巢狀 | 提早返回 |
| 跨模組引用 > 3 | 重新設計 |

### 提早返回

```javascript
// ❌ 多層巢狀
if (order) {
  if (order.items.length > 0) {
    if (order.status === 'pending') { ... }
  }
}

// ✅ 提早返回
if (!order) return;
if (order.items.length === 0) return;
if (order.status !== 'pending') return;
// 處理邏輯...
```

---

# ═══════════════════════════════════════════
# 第三部分：頁面設計規範
# ═══════════════════════════════════════════

## 🍞 頁面導航規範

### 每個頁面必須有

| 元素 | 必要性 |
|------|--------|
| 麵包屑（首頁 > 分類 > 當前頁） | ✅ 必要 |
| 引導列/導航列 | ✅ 必要 |
| 返回上一頁按鈕 | ✅ 必要 |
| 頁面標題 | ✅ 必要 |

---

## 🎨 視覺模板系統

```
client/src/
├── layouts/              # 版面配置
│   ├── MainLayout.jsx    # 主要版面
│   ├── AuthLayout.jsx    # 登入版面
│   ├── AdminLayout.jsx   # 管理後台
│   └── BlankLayout.jsx   # 空白版面（錯誤頁用）
├── components/           # 共用元件
│   ├── Header.jsx
│   ├── Footer.jsx
│   ├── Breadcrumb.jsx
│   └── BackButton.jsx
├── pages/
│   └── errors/           # 錯誤頁面（400/401/403/404/500）
└── themes/               # 主題（CSS 變數）
    ├── default/
    └── dark/
```

---

## 🌐 開放型網站（SEO + AI 優化）

```
# robots.txt
User-agent: *
Allow: /
User-agent: GPTBot
Allow: /
User-agent: Claude-Web
Allow: /
Sitemap: https://yoursite.com/sitemap.xml
```

## 🔒 封閉型網站（防爬蟲）

```
# robots.txt
User-agent: *
Disallow: /
```

---

# ═══════════════════════════════════════════
# 第四部分：第三方服務整合
# ═══════════════════════════════════════════

## 🔐 帳號管理

| 方式 | 服務 |
|------|------|
| Email/密碼 | Firebase Auth |
| Google | Firebase Auth |
| LINE | LINE Login API |

## 📷 Cloudinary 圖片管理

## 📧 Resend 信件服務

### 環境變數（.env.example）

```bash
# Firebase
FIREBASE_API_KEY=xxx
FIREBASE_AUTH_DOMAIN=xxx
FIREBASE_PROJECT_ID=xxx

# LINE Login
LINE_CHANNEL_ID=xxx
LINE_CHANNEL_SECRET=xxx
LINE_CALLBACK_URL=xxx

# Cloudinary
CLOUDINARY_CLOUD_NAME=xxx
CLOUDINARY_API_KEY=xxx
CLOUDINARY_API_SECRET=xxx

# Resend
RESEND_API_KEY=xxx

# JWT
JWT_SECRET=xxx

# 資料庫
DATABASE_URL=xxx
```

---

# ═══════════════════════════════════════════
# 第五部分：子代理系統（Agents）
# ═══════════════════════════════════════════

## 🤖 子代理（from everything-claude-code）

Claude Code 可以派出專業子代理處理特定任務。
子代理設定檔位於：`~/.claude/agents/` 或 `.claude/agents/`

### 可用的子代理

| 代理 | 用途 | 觸發方式 | 說明 |
|------|------|---------|------|
| **planner** | 功能規劃 | `/plan` 指令 | 開發前先規劃，確認後才動手 |
| **architect** | 架構設計 | 自動/手動 | 選擇技術棧、設計系統架構 |
| **code-reviewer** | 程式碼審查 | `/code-review` | 檢查品質、安全性、可維護性 |
| **security-reviewer** | 安全分析 | 自動/手動 | 漏洞掃描、安全建議 |
| **tdd-guide** | 測試驅動 | `/tdd` | 先寫測試、再寫程式 |
| **build-error-resolver** | 編譯排錯 | `/build-fix` | 快速解決編譯錯誤 |
| **refactor-cleaner** | 重構清理 | `/refactor-clean` | 清理無用程式碼、減少技術債 |
| **doc-updater** | 文件更新 | 自動 | 程式碼改了，文件也自動更新 |
| **e2e-runner** | E2E 測試 | `/e2e` | Playwright 端對端測試 |

### 子代理使用原則

```
1. 複雜功能開發前 → 先用 planner 規劃
2. 新系統設計時 → 用 architect 做架構決策
3. 功能完成後 → 用 code-reviewer 審查
4. 部署前 → 用 security-reviewer 掃描
5. 重構時 → 用 refactor-cleaner 清理
```

### planner 規劃流程

```
你說：「我要加一個會員通知功能」
       │
       ▼
planner 產出：
  1. 需求確認
  2. 架構變更
  3. 實作步驟（分階段）
  4. 風險評估
  5. 預估時間
       │
       ▼
你確認：「好，開始做」
       │
       ▼
才開始寫程式碼
```

### code-reviewer 審查項目

```
自動檢查：
✅ 程式碼品質（命名、結構）
✅ 安全性（SQL 注入、XSS）
✅ 效能（N+1 查詢、記憶體）
✅ 可維護性（耦合度、複雜度）
✅ 測試覆蓋率
✅ 行數是否超標
```

---

# ═══════════════════════════════════════════
# 第六部分：快捷指令系統（Commands）
# ═══════════════════════════════════════════

## ⚡ 快捷指令

指令設定檔位於：`~/.claude/commands/` 或 `.claude/commands/`

| 指令 | 功能 | 使用時機 |
|------|------|---------|
| `/plan` | 功能規劃 | 開發新功能前 |
| `/tdd` | 測試驅動開發 | 寫新功能 |
| `/code-review` | 程式碼審查 | 功能完成後 |
| `/build-fix` | 修復編譯錯誤 | 編譯失敗時 |
| `/refactor-clean` | 清理重構 | 定期維護 |
| `/e2e` | 端對端測試 | 測試流程 |
| `/learn` | 提取經驗 | 學習模式 |
| `/verify` | 驗證迴圈 | 確認品質 |
| `/checkpoint` | 儲存進度 | 重要節點 |

### 使用範例

```bash
# 開發新功能
> /plan 新增會員通知功能

# 寫程式
> /tdd 建立通知服務

# 完成後審查
> /code-review

# 修復錯誤
> /build-fix

# 清理程式碼
> /refactor-clean
```

---

# ═══════════════════════════════════════════
# 第七部分：自動化鉤子系統（Hooks）
# ═══════════════════════════════════════════

## 🪝 自動觸發鉤子

鉤子設定在 `~/.claude/settings.json` 或 `.claude/settings.json`

### 鉤子類型

| 鉤子 | 觸發時機 | 功能 |
|------|---------|------|
| **SessionStart** | 開啟 Claude Code | 載入上次進度、記憶 |
| **SessionEnd (Stop)** | 關閉 Claude Code | 儲存當前狀態 |
| **PreToolUse** | 執行工具前 | 檢查危險操作 |
| **PostToolUse** | 執行工具後 | 驗證結果 |
| **PreCompact** | 上下文壓縮前 | 儲存重要資訊 |

### 記憶持久化鉤子

```
開啟 Claude Code
       │
       ▼
SessionStart 鉤子
├── 載入最近 7 天的工作記錄
├── 載入已學習的技能
└── 顯示：「找到 X 個最近工作記錄」
       │
       ▼
（你開始工作...）
       │
       ▼
關閉 Claude Code
       │
       ▼
SessionEnd 鉤子
├── 儲存今天的工作記錄
├── 記錄結束時間
└── 下次自動載入
```

### 安全鉤子

```
你或 Claude 要執行指令
       │
       ▼
PreToolUse 鉤子檢查
├── rm -rf? → ❌ 阻止
├── 修改 .env? → ⚠️ 警告
├── git push? → ⚠️ 確認
├── console.log 殘留? → ⚠️ 提醒移除
└── 安全? → ✅ 放行
```

### 鉤子設定範例

```json
{
  "hooks": {
    "SessionStart": [{
      "matcher": "*",
      "hooks": [{
        "type": "command",
        "command": "node scripts/hooks/session-start.js"
      }]
    }],
    "Stop": [{
      "matcher": "*",
      "hooks": [{
        "type": "command",
        "command": "node scripts/hooks/session-end.js"
      }]
    }],
    "PreToolUse": [{
      "matcher": "tool == \"Bash\"",
      "hooks": [{
        "type": "command",
        "command": "node scripts/hooks/check-dangerous.js"
      }]
    }]
  }
}
```

---

# ═══════════════════════════════════════════
# 第八部分：技能庫（Skills）
# ═══════════════════════════════════════════

## 📚 技能庫

技能設定檔位於：`~/.claude/skills/` 或 `.claude/skills/`

### 可用技能

| 技能 | 說明 | 自動啟用 |
|------|------|---------|
| **coding-standards** | 程式碼品質標準 | ✅ 永遠啟用 |
| **backend-patterns** | API、資料庫、快取模式 | ✅ 後端開發時 |
| **frontend-patterns** | React、Next.js 模式 | ✅ 前端開發時 |
| **tdd-workflow** | 測試驅動開發流程 | `/tdd` 觸發 |
| **security-review** | 安全檢查清單 | 部署前觸發 |
| **continuous-learning** | 持續學習你的模式 | ✅ 永遠啟用 |

### 持續學習系統

```
你寫程式碼
     │
     ▼
Claude 觀察你的模式
├── 你常用的命名方式
├── 你偏好的架構
├── 你常犯的錯誤
     │
     ▼
自動儲存到 skills/learned/
     │
     ▼
下次自動套用你的習慣
```

### TDD 工作流程

```
/tdd 觸發：

1. 定義介面
   ↓
2. 寫失敗的測試（RED）
   ↓
3. 寫最少的程式讓測試通過（GREEN）
   ↓
4. 重構改善（IMPROVE）
   ↓
5. 確認覆蓋率 > 80%
```

---

# ═══════════════════════════════════════════
# 第九部分：安全規則（Rules）
# ═══════════════════════════════════════════

## 🛡️ 安全規則

規則設定檔位於：`~/.claude/rules/`（全域）或 `.claude/rules/`（專案）

### 必遵守的安全規則

```
1. 永遠不要把密碼、API 金鑰寫在程式碼裡 → 用 .env
2. 永遠不要把 .env 推到 GitHub → .gitignore
3. 所有使用者輸入都必須驗證
4. SQL 查詢必須使用參數化查詢
5. 前端不要暴露後端邏輯
6. 正式版必須移除 console.log
7. 正式版必須使用 HTTPS
8. 密碼必須加密儲存（bcrypt）
```

### 程式碼風格規則

```
1. 偏好不可變（immutable）設計
2. 多個小檔案比一個大檔案好
3. 200-400 行典型，800 行上限
4. 所有 export 都透過 index.js
5. 禁止使用 var，用 const/let
6. 禁止 any 型別（TypeScript）
```

### 測試規則

```
1. 新功能必須有測試
2. 修 bug 前先補測試
3. 測試覆蓋率 > 80%
4. 測試檔案放在同一目錄
5. 本地測試通過才能 commit
```

### Git 規則

```
1. 使用 conventional commits 格式
2. 本地測試通過才能推送
3. 不直接推送到 main 分支
4. 重要版本用 tag 標記
```

---

# ═══════════════════════════════════════════
# 第十部分：自動化檢測系統
# ═══════════════════════════════════════════

## 🔍 自動化檢測

### 檢測項目

| 項目 | 頻率 | 工具 |
|------|------|------|
| ESLint 品質檢查 | 每次 commit | `npm run lint` |
| 程式碼行數 | 每次 commit | `npm run check:lines` |
| 模組耦合度 | 每週 | `npm run check:coupling` |
| 安全性掃描 | 每週 | `npm audit` |
| AI 優化建議 | 每週 | `npm run review:ai` |
| 測試覆蓋率 | 每次 commit | `npm test` |

### GitHub Actions 自動執行

```yaml
# .github/workflows/auto-review.yml
name: 🔍 自動程式碼檢測
on:
  push:
    branches: [main, develop]
  schedule:
    - cron: '0 1 * * 1'  # 每週一

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
      - run: npm ci
      - run: npm run lint
      - run: npm run check:lines
      - run: npm run check:coupling
      - run: npm test
```

### 完整檢測指令

```bash
npm run review:full
# = lint + check:lines + check:coupling + review:ai + generate-report
```

---

## 🚨 健康檢查 API

```javascript
GET /health        → { status: 'ok', uptime: xxx }
GET /health/detail → { database: 'ok', redis: 'ok', ... }
```

---

# ═══════════════════════════════════════════
# 第十一部分：MCP 整合（進階）
# ═══════════════════════════════════════════

## 🔌 MCP 伺服器整合

MCP 設定在 `~/.claude.json` 或 `.mcp.json`

### 建議的 MCP 服務

| 服務 | 用途 | 必要性 |
|------|------|--------|
| GitHub | 管理 repo、issue、PR | ⭐⭐⭐ 推薦 |
| Supabase | 資料庫操作 | 看專案需求 |
| Vercel | 部署管理 | 看部署方式 |

### ⚠️ MCP 注意事項

```
重要：不要同時開太多 MCP！

200k context window → 開太多會縮到 70k

建議：
- 設定 20-30 個 MCP
- 同時啟用不超過 10 個
- 用 disabledMcpServers 關閉不用的
```

---

# ═══════════════════════════════════════════
# 第十二部分：完整專案結構
# ═══════════════════════════════════════════

## 📁 完整專案結構

```
[PROJECT_NAME]/
│
├── 📄 CLAUDE.md              # Claude Code 規則（本檔案）
├── 📄 PROGRESS.md            # 進度追蹤
├── 📄 README.md              # 專案說明
├── 📄 .env.example           # 環境變數範例
├── 📄 .gitignore
├── 📄 package.json
│
├── 📁 .claude/               # Claude Code 專案設定
│   ├── settings.json         # 鉤子、權限
│   ├── 📁 agents/            # 專案子代理
│   ├── 📁 commands/          # 專案指令
│   ├── 📁 skills/            # 專案技能
│   └── 📁 rules/             # 專案規則
│
├── 📁 .github/
│   └── workflows/
│       └── auto-review.yml   # 自動化檢測
│
├── 📁 scripts/               # 腳本
│   ├── check-lines.js
│   ├── check-coupling.js
│   ├── ai-review.js
│   ├── generate-report.js
│   ├── backup.sh
│   └── 📁 hooks/             # 鉤子腳本
│       ├── session-start.js
│       ├── session-end.js
│       └── check-dangerous.js
│
├── 📁 reports/               # 檢測報告
│
├── 📁 src/
│   ├── app.js
│   ├── server.js
│   ├── 📁 config/
│   │   ├── database.js
│   │   ├── auth.js
│   │   ├── cloudinary.js
│   │   └── resend.js
│   ├── 📁 modules/           # 功能模組
│   │   ├── auth/
│   │   ├── users/
│   │   ├── upload/
│   │   ├── email/
│   │   └── ...
│   ├── 📁 middlewares/
│   │   ├── auth.js
│   │   ├── errorHandler.js
│   │   ├── validate.js
│   │   └── antiBot.js
│   ├── 📁 utils/
│   └── 📁 validators/
│
├── 📁 client/
│   └── 📁 src/
│       ├── 📁 layouts/
│       ├── 📁 components/
│       ├── 📁 pages/
│       │   └── errors/
│       └── 📁 themes/
│
├── 📁 database/
├── 📁 docs/
│   ├── ARCHITECTURE.md
│   ├── API.md
│   └── DATABASE.md
└── 📁 logs/
```

---

# ═══════════════════════════════════════════
# 第十三部分：禁止事項與強制要求
# ═══════════════════════════════════════════

## ❌ 絕對禁止

### 程式碼
- **禁止** 單一檔案超過 1000 行
- **禁止** 單一函式超過 50 行
- **禁止** 超過 3 層巢狀
- **禁止** 跨模組直接引用內部檔案
- **禁止** 複製貼上程式碼
- **禁止** 使用 var
- **禁止** 使用 any（TypeScript）

### 安全
- **禁止** 密碼、金鑰寫在程式碼
- **禁止** .env 推到 GitHub
- **禁止** 不驗證使用者輸入
- **禁止** console.log 留在正式版
- **禁止** 明文儲存密碼

### 備份
- **禁止** 開發中半成品推到 GitHub
- **禁止** 超過一週不 push
- **禁止** 不測試就部署
- **禁止** 不標記重要版本

### 命名
- **禁止** 無意義命名（data, temp, a）
- **禁止** v2, new, enhanced 命名

---

## ✅ 強制要求

### 每次開發
1. ✅ 開始前 `git pull`
2. ✅ 讀取 PROGRESS.md
3. ✅ 頻繁 commit（本地）
4. ✅ 功能完成才 push
5. ✅ 更新 PROGRESS.md
6. ✅ 複雜功能先用 `/plan` 規劃

### 每次完成功能
1. ✅ 執行 `/code-review`
2. ✅ 測試通過
3. ✅ 沒有超過行數限制
4. ✅ 沒有 console.log 殘留

### 每次部署
1. ✅ 本地測試通過
2. ✅ 執行 security-reviewer
3. ✅ 確認環境變數
4. ✅ 部署後驗證

### 每週
1. ✅ 至少 push 一次
2. ✅ 執行 `npm run review:full`
3. ✅ 處理檢測報告
4. ✅ 清理本地舊備份

---

## 📋 每日開發檢查清單

```
開始開發：
□ git pull 取得最新
□ 讀取 PROGRESS.md
□ 複雜功能先 /plan

開發中：
□ 完成小功能就 commit
□ 訊息格式正確
□ 行數不超標

結束開發：
□ /code-review 檢查
□ 更新 PROGRESS.md
□ 功能完成則 push
```

---

## 🤖 Claude Code 初始化完成訊息

```
✅ 專案「[PROJECT_NAME]」初始化完成！

📋 CLAUDE.md 規則已啟用（v5.0）
📊 PROGRESS.md 進度追蹤已建立
🔍 自動化檢測已設定
🤖 子代理系統已就緒
⚡ 快捷指令已載入
🪝 自動化鉤子已啟用
📚 技能庫已載入
🛡️ 安全規則已啟用
🌐 語言：繁體中文

開發流程：
1. /plan 規劃功能
2. 本地開發 → 頻繁 commit
3. /code-review 審查
4. 功能完成 → push 到 GitHub
5. Coolify 自動部署

可用指令：
/plan /tdd /code-review /build-fix
/refactor-clean /e2e /learn /verify
```

---

**📏 程式碼簡潔：800 行以內**
**🧩 模組獨立：低耦合、高內聚**
**💾 備份策略：本地頻繁、雲端乾淨**
**🚀 部署流程：push → GitHub → Coolify → 上線**
**🔍 自動檢測：GitHub Actions + AI 分析**
**🤖 子代理：規劃、審查、安全、重構**
**⚡ 快捷指令：/plan /tdd /review /fix**
**🪝 自動鉤子：記憶持久化、安全檢查**
**📚 技能庫：持續學習你的開發模式**
**🌐 全程中文溝通**

---

<!-- CLAUDE_CODE_INIT_END -->
